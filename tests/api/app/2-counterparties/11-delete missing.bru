delete {
    url: {{baseUrl}}/app/users?id=missing
}
headers {
    Content-Type: application/json
    X-API-Key: {{apiKey}}
}
tests {
    test("Delete a non-existing counterpary should fail", function() {
        expect(res.status).to.equal(404);
    })
}