get {
    url: {{baseUrl}}/app/counterparties?id={{counterparty1Id}}
}
headers {
    Content-Type: application/json
    X-API-Key: {{apiKey}}
}
tests {
    test("List a recently deleted counterparty should fail", function() {
        expect(res.status).to.equal(404);
    })
}