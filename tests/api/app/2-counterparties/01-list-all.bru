get {
    url: {{baseUrl}}/app/counterparties
}
headers {
    Content-Type: application/json
    X-API-Key: {{apiKey}}
}
tests {
    test("List all counterparties should return empty array", function() {
        expect(res.body).to.be.an("array").that.is.empty;
    })
}