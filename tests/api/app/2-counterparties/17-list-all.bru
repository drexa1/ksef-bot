get {
    url: {{baseUrl}}/app/users
}
headers {
    Content-Type: application/json
    X-API-Key: {{apiKey}}
}
tests {
    test("List all counterparties at the end should return empty array", function() {
        expect(res.body).to.be.an("array").that.is.empty;
    })
}