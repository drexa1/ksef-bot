get {
    url: {{baseUrl}}/app/counterparties?id=missing
}
headers {
    Content-Type: application/json
    X-API-Key: {{apiKey}}
}
tests {
    test("List a non-existing counterparty should fail", function() {
        expect(res.status).to.equal(404);
    })
}