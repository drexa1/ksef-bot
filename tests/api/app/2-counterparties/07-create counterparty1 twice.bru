post {
    url: {{baseUrl}}/app/counterparties
}
headers {
    Content-Type: application/json
    X-API-Key: {{apiKey}}
}
script:pre-request {
    const fs = require("fs");
    const body = fs.readFileSync("../testresources/counterparties/counterparty-2.json", "utf8");
    req.setBody(JSON.parse(body));
}
tests {
    test("Second attempt to create an existing counterparty should trigger PK violation error", function() {
        expect(res.status).to.equal(409);
    })
}