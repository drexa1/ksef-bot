post {
    url: {{baseUrl}}/app/invoices
    body: multipart-form
    auth: inherit
}
headers {
    Content-Type: application/json
    X-API-Key: {{apiKey}}
}
script:pre-request {
    const fs = require("fs");
    const xml = fs.readFileSync("../testresources/ksef/sales/6751577878-20260629-514966000000-3F.xml", "utf8");
    const body =
        `--ksefInvoice\n` +
        `Content-Disposition: form-data; name="file"; filename="sales-invoice-1.xml"\n` +
        `Content-Type: application/xml\n` +
        `\n` +
        `${xml}\n` +
        `--ksefInvoice\n` +
        `Content-Disposition: form-data; name="notes"\n` +
        `\n` +
        `Test notes\n` +
        `--ksefInvoice--`;
    req.setHeader("Content-Type", "multipart/form-data; boundary=ksefInvoice");
    req.setBody(body);
}
tests {
    test("Second attempt to create an existing sales invoice should trigger PK violation error", function() {
        expect(res.status).to.equal(409);
    })
}