get {
    url: {{baseUrl}}/app/invoices
}
headers {
    Content-Type: application/json
    X-API-Key: {{apiKey}}
}
tests {
    test("List all invoices should return empty array", function() {
        expect(res.body).to.be.an("array").that.is.empty;
    })
}