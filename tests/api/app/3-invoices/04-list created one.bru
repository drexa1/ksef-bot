get {
    url: {{baseUrl}}/app/invoices?id={{salesInvoiceId}}
}
headers {
    Content-Type: application/json
    X-API-Key: {{apiKey}}
}
tests {
    test("List the recently created invoice", function() {
        expect(res.body.id).to.equal(bru.getCollectionVar("counterparty1Id"));
    })
}