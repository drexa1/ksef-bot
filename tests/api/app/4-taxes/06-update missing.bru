put {
    url: {{baseUrl}}/app/taxes?from=missing&to=missing
}
headers {
    Content-Type: application/json
    X-API-Key: {{apiKey}}
}
tests {
    test("Update a non-existing tax record raise invalid dates", function() {
        expect(res.status).to.equal(400);
    })
}