get {
    url: {{baseUrl}}/app/taxes
}
headers {
    Content-Type: application/json
    X-API-Key: {{apiKey}}
}
tests {
    test("List all tax records at the end should return empty array", function() {
        expect(res.body).to.be.an("array").that.is.empty;
    })
}