delete {
    url: {{baseUrl}}/app/taxes?from=missing&to=missing
}
headers {
    Content-Type: application/json
    X-API-Key: {{apiKey}}
}
tests {
    test("Delete a non-existing tax record should raise invalid dates", function() {
        expect(res.status).to.equal(400);
    })
}