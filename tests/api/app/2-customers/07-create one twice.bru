post {
    url: {{baseUrl}}/app/customers
}
headers {
    Content-Type: application/json
    X-API-Key: {{apiKey}}
}
script:pre-request {
    const fs = require("fs");
    const body = fs.readFileSync("../testresources/customers/customer-2.json", "utf8");
    req.setBody(JSON.parse(body));
}
tests {
    test("Second attempt to create an existing customer should trigger PK violation error", function() {
        expect(res.status).to.equal(409);
    })
}