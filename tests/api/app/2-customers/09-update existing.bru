put {
    url: {{baseUrl}}/app/customers
}
headers {
    Content-Type: application/json
    X-API-Key: {{apiKey}}
}
script:pre-request {
    req.setBody(JSON.stringify({ id: bru.getCollectionVar("customer1Id"), name: "edited" }));
}
tests {
    test("Update a recently created customer should succeed", function() {
        expect(res.body.id).to.equal(bru.getCollectionVar("customer1Id"));
    })
}