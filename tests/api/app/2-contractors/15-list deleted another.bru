get {
    url: {{baseUrl}}/app/customers?id={{customer2Id}}
}
headers {
    Content-Type: application/json
    X-API-Key: {{apiKey}}
}
tests {
    test("List a recently deleted customer should fail", function() {
        expect(res.status).to.equal(404);
    })
}