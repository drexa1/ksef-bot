get {
    url: {{baseUrl}}/app/contractors?id=missing
}
headers {
    Content-Type: application/json
    X-API-Key: {{apiKey}}
}
tests {
    test("List a non-existing contractor should fail", function() {
        expect(res.status).to.equal(404);
    })
}