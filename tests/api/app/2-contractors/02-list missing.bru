get {
    url: {{baseUrl}}/app/customers?id=missing
}
headers {
    Content-Type: application/json
    X-API-Key: {{apiKey}}
}
tests {
    test("List a non-existing customer should fail", function() {
        expect(res.status).to.equal(404);
    })
}