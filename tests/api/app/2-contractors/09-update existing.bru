put {
    url: {{baseUrl}}/app/contractors
}
headers {
    Content-Type: application/json
    X-API-Key: {{apiKey}}
}
script:pre-request {
    req.setBody(JSON.stringify({ id: bru.getCollectionVar("contractor1Id"), name: "edited" }));
}
tests {
    test("Update a recently created contractor should succeed", function() {
        expect(res.body.id).to.equal(bru.getCollectionVar("contractor1Id"));
    })
}