delete {
    url: {{baseUrl}}/app/customers?id={{customer1Id}}
}
headers {
    Content-Type: application/json
    X-API-Key: {{apiKey}}
}
tests {
    test("Delete a recently updated customer should succeed", function() {
        expect(res.body.id).to.equal(bru.getCollectionVar("customer1Id"));
    })
}