put {
    url: {{baseUrl}}/app/contractors
}
headers {
    Content-Type: application/json
    X-API-Key: {{apiKey}}
}
script:pre-request {
    req.setBody(JSON.stringify({ id: "missing", name: "nevermind" }));
}
tests {
    test("Update a missing contractor should fail", function() {
        expect(res.status).to.equal(404);
    })
}