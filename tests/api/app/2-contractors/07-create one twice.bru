post {
    url: {{baseUrl}}/app/contractors
}
headers {
    Content-Type: application/json
    X-API-Key: {{apiKey}}
}
script:pre-request {
    const fs = require("fs");
    const body = fs.readFileSync("../testresources/contractors/contractor-2.json", "utf8");
    req.setBody(JSON.parse(body));
}
tests {
    test("Second attempt to create an existing contractor should trigger PK violation error", function() {
        expect(res.status).to.equal(409);
    })
}