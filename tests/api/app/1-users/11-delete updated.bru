delete {
    url: {{baseUrl}}/app/users?email={{userId}}
}
headers {
    Content-Type: application/json
    X-API-Key: {{apiKey}}
}
tests {
    test("Delete a recently updated user should succeed", function() {
        expect(res.body.id).to.equal(bru.getCollectionVar("userId"));
    })
}