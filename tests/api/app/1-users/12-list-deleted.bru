get {
    url: {{baseUrl}}/app/users?email={{userEmail}}
}
headers {
    Content-Type: application/json
    X-API-Key: {{apiKey}}
}
tests {
    test("List a recently deleted user should fail", function() {
        expect(res.status).to.equal(404);
    })
}