get {
    url: {{baseUrl}}/app/users
}
headers {
    Content-Type: application/json
    X-API-Key: {{apiKey}}
}
tests {
    test("List all users should at least return admin users", function() {
        expect(res.body.filter(u => u.tier === 0).length).to.be.at.least(1);
    })
}