get {
    url: {{baseUrl}}/app/users?email=missing
}
headers {
    Content-Type: application/json
    X-API-Key: {{apiKey}}
}
tests {
    test("List non-existing user should fail", function() {
        expect(res.status).to.equal(404)
    })
}