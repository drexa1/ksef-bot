get {
    url: {{baseUrl}}/app/users
}
headers {
    Content-Type: application/json
    X-API-Key: {{apiKey}}
}
tests {
    test("List all should always return the admin user", function() {
        expect(res.body.find(u => u.tier === 0)).to.not.be.undefined;
    })
}