get {
    url: {{baseUrl}}/app/users
}
headers {
    Content-Type: application/json
    X-API-Key: {{apiKey}}
}
tests {
    test("List all users should at least return admin users", function() {
        expect(res.body.every(u => u.tier === 0)).to.be.true;
    })
}