get {
    url: {{baseUrl}}/app/users?email={{userEmail}}
}
headers {
    Content-Type: application/json
    X-API-Key: {{apiKey}}
}
tests {
    test("List a recently updated user should show the edition", function() {
        expect(res.body.firstName).to.equal("edited");
    })
}